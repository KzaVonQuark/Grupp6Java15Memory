package application;

public class Test {
	
	/*
	 * Rules rules = new Rules(); GameBoard gb = new GameBoard(); Player p = new
	 * Player("Gurra");
	 * 
	 * void playerTurn(boolean gotPair) {
	 * 
	 * // Reads first element in queue. if (gotPair == true) {
	 * gb.getQ().peek().setPoints(gb.getQ().peek().getPoints() + 1);
	 * gb.getQ().peek().setMoves(gb.getQ().peek().getMoves() + 1); }
	 * 
	 * // Reads, removes and put element last in queue else {
	 * gb.getQ().peek().setMoves(gb.getQ().peek().getMoves() + 1);
	 * gb.getQ().add(gb.getQ().poll()); } }
	 */
}
